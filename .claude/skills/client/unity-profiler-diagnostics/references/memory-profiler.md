# Memory Profiler Package

Covers SKILL.md step 8 (real leak/growth investigation via snapshot diffing).

## Manual (package)
- [Memory Profiler introduction](https://docs.unity3d.com/Packages/com.unity.memoryprofiler@1.1/manual/memory-profiler-introduction.html) — snapshots, native + managed allocations, comparing two snapshots to find growth between them.

Note: version-substitute the `@1.1` segment for the package version actually installed in the project (`Window > Package Manager` or `manifest.json`); the page structure has been stable across recent Memory Profiler package releases.
