# Root Links

Root/index pages. Follow their own in-page navigation for anything not covered by the other files in this folder — this is the canonical source this skill is built from, as given by the user.

- [Unity Manual — Unity Profiler](https://docs.unity3d.com/Manual/Profiler.html)

Note: Unity's Manual pages under this root are largely unversioned (`docs.unity3d.com/Manual/...`) and resolve to the current LTS/Tech Stream docs; a version-pinned path (e.g. `docs.unity3d.com/6000.5/Documentation/Manual/...`) also works if a specific installed Editor version's wording needs to be checked.
