# Root Links

Root/index pages. Follow their own in-page navigation for anything not covered by the other files in this folder — this is the canonical source this skill is built from, as given by the user.

- [Unity Manual — Write multithreaded code with the job system](https://docs.unity3d.com/6000.5/Documentation/Manual/job-system.html)

Related root indexes discovered while building this skill (same doc family, not separately given by the user):
- [Unity Manual — Burst](https://docs.unity3d.com/Manual/com.unity.burst.html) — links out to the versioned Burst package manual.
- [Unity Scripting API — IJob](https://docs.unity3d.com/ScriptReference/Unity.Jobs.IJob.html) — `Unity.Jobs` namespace entry point.

Note: the Manual pages under `job-system-*` are largely unversioned (`docs.unity3d.com/Manual/...`) and resolve to the current LTS/Tech Stream docs; a version-pinned path (e.g. `docs.unity3d.com/6000.5/Documentation/Manual/...`) also works if a specific installed Editor version's wording needs to be checked.
